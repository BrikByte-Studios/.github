# =============================================================================
# BrikByteOS — Reusable Go Unit Test Workflow (with governed sharding)
# -----------------------------------------------------------------------------
# WBS ID : PIPE-TEST-UNIT-CI-BUILD-002
#
# Matrix plan integration:
#   - Uses BrikPipe Matrix Plan composite action to compute shards deterministically
#   - Parallel rules live in:
#       BrikByte-Studios/.github/.github/actions/matrix-plan/parallel-matrix.yml
# =============================================================================
name: "BrikByteOS — Go Unit Tests"

on:
  workflow_call:
    inputs:
      go-version:
        description: "Go version (must align with runtime matrix, e.g. 1.22.x)"
        required: false
        type: string
        default: "1.22.x"
      working-directory:
        description: "Path to Go module root (Makefile + go.mod)"
        required: false
        type: string
        default: "."

      # ------------------------------
      # Governed parallelism (optional)
      # ------------------------------
      override_shards:
        description: "Optional requested shard count (clamped to caps)."
        required: false
        type: string
        default: ""
      matrix_config_path:
        description: >
          Optional path to parallel-matrix.yml.
          Leave empty to use default inside BrikByte-Studios/.github.
        required: false
        type: string
        default: ""

      parallel_mode:
        description: "Parallel mode: static|dynamic (default static)"
        required: false
        type: string
        default: "static"

      parallel_enabled:
        description: "Enable parallel shard execution (true|false)"
        required: false
        type: string
        default: "true"

permissions:
  contents: read

jobs:
  # ---------------------------------------------------------------------------
  # 0) Plan matrix (must be a separate job to use outputs in strategy.matrix)
  # ---------------------------------------------------------------------------
  plan:
    name: "Plan matrix (go unit)"
    runs-on: ubuntu-latest
    outputs:
      matrix_json: ${{ steps.plan.outputs.matrix_json }}
    steps:
      - name: "Compute matrix plan (unit)"
        id: plan
        uses: BrikByte-Studios/.github/.github/actions/matrix-plan@main
        with:
          test_type: "unit"
          config_path: ${{ inputs.matrix_config_path }}
          override_shards: ${{ inputs.override_shards }}

  # ---------------------------------------------------------------------------
  # 1) Execute tests (sharded)
  # ---------------------------------------------------------------------------
  test:
    name: "Go: Unit Tests (sharded)"
    runs-on: ubuntu-latest
    needs: plan

    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.plan.outputs.matrix_json) }}

    env:
      LANGUAGE: go
      GO_VERSION: ${{ inputs.go-version != '' && inputs.go-version || '1.22.x' }}
      WORKING_DIRECTORY: ${{ inputs.working-directory != '' && inputs.working-directory || '.' }}
      GOFLAGS: "-mod=readonly"

      # shard metadata (defined by matrix-plan output)
      SHARD_INDEX: ${{ matrix.shard }}
      SHARD_TOTAL: ${{ strategy.job-total }}

      # standardized contract for Makefile runners
      UNIT_SHARD: ${{ matrix.shard }}
      UNIT_SHARD_TOTAL: ${{ strategy.job-total }}

      PARALLEL_MODE: ${{ inputs.parallel_mode }}
      PARALLEL: ${{ inputs.parallel_enabled }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify Go test layout
        run: |
          echo "📁 Working directory: ${WORKING_DIRECTORY}"

          if [ ! -d "${WORKING_DIRECTORY}" ]; then
            echo "❌ WORKING_DIRECTORY '${WORKING_DIRECTORY}' does not exist."
            exit 1
          fi

          if [ ! -f "${WORKING_DIRECTORY}/go.mod" ]; then
            echo "❌ go.mod not found in ${WORKING_DIRECTORY}."
            exit 1
          fi

          if [ ! -f "${WORKING_DIRECTORY}/Makefile" ]; then
            echo "❌ Makefile not found in ${WORKING_DIRECTORY}."
            echo "   Expected 'make test' mapping to 'go test ./...'."
            exit 1
          fi

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: "Cache Go modules & build cache"
        uses: BrikByte-Studios/.github/.github/actions/cache-go-deps@main
        with:
          go-version: ${{ env.GO_VERSION }}
          project-path: ${{ env.WORKING_DIRECTORY }}

      # -------------------------------------------------------------------
      # Install go-junit-report so Makefile can safely emit out/junit.xml
      # -------------------------------------------------------------------
      - name: Install go-junit-report
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          set -euo pipefail
          echo "📦 Installing go-junit-report..."
          go install github.com/jstemmer/go-junit-report/v2@latest
          GOPATH="$(go env GOPATH)"
          echo "📁 GOPATH is: ${GOPATH}"
          echo "${GOPATH}/bin" >> "$GITHUB_PATH"
          echo "✅ go-junit-report installed and added to PATH"

      - name: Compute shard env (1-based for Makefile)
        shell: bash
        run: |
          set -euo pipefail

          SHARD0="${SHARD_INDEX}"
          TOTAL="${SHARD_TOTAL}"

          # Convert to 1-based (for your Node Makefile/shard-tests.cjs)
          SHARD1=$((SHARD0 + 1))

          {
            echo "UNIT_SHARD=${SHARD1}"
            echo "UNIT_SHARD_TOTAL=${TOTAL}"
          } >> "$GITHUB_ENV"

          echo "[SHARD] 0-based=${SHARD0} -> 1-based=${SHARD1} / total=${TOTAL}"

      - name: Parallel runner (serial/static/dynamic)
        id: pr
        uses: BrikByte-Studios/.github/.github/actions/parallel-runner@main
        with:
          parallel_enabled: ${{ env.PARALLEL }}
          parallel_mode: ${{ env.PARALLEL_MODE }}
          shard_index: ${{ matrix.shard }}          # 0-based
          shard_total: ${{ strategy.job-total }}
          selection_mode: "file-split"
          working_directory: ${{ env.WORKING_DIRECTORY }}
          test_glob: "**/*.go"
          out_list: "${{ env.WORKING_DIRECTORY }}/out/shard-files.txt"
          # items_file: "${{ env.WORKING_DIRECTORY }}/out/test-items.txt"
          # optional knobs:
          # allow_empty_items: true
          # selection_mode: file-split|shard-map|framework-native

      - name: Run Go unit tests (make test)
        if: ${{ steps.pr.outputs.is_empty_shard != 'true' }}
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          set -euo pipefail

          echo "🧪 [TEST] Language   : ${LANGUAGE}"
          echo "🧪 [TEST] Runtime    : Go ${GO_VERSION}"
          echo "🧪 [TEST] Directory  : ${PWD}"
          echo "🧪 [TEST] Shard      : ${SHARD_INDEX}/${SHARD_TOTAL}"
          echo "🧪 [TEST] Command    : make test"
          echo "📌 TEST_ITEMS_FILE="${{ steps.pr.outputs.selection_path }}""
          echo "🧪 [TEST] Status     : STARTING"

          START_TS=$(date +%s)
          export UNIT_SHARD UNIT_SHARD_TOTAL
          export TEST_ITEMS_FILE="${{ steps.pr.outputs.selection_path }}"
          make test

          END_TS=$(date +%s)
          DURATION=$((END_TS - START_TS))

          echo "✅ [TEST] Status     : COMPLETED"
          echo "⏱  [TEST] Duration   : ${DURATION} seconds"

      # If this shard is empty, succeed quickly.
      - name: Empty shard (nothing to run)
        if: ${{ steps.pr.outputs.is_empty_shard == 'true' }}
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          set -euo pipefail
          echo "ℹ️ Empty shard; skipping tests."
          mkdir -p out
          : > out/junit.xml
          : > coverage.out || true

      # -----------------------------------------------------------------------
      # X) Collect coverage (Go: go test -coverprofile=coverage.out)
      # -----------------------------------------------------------------------
      - name: Normalize coverage → coverage.json
        uses: BrikByte-Studios/.github/.github/actions/coverage-merge@main
        with:
          language: ${{ env.LANGUAGE }}
          working-directory: ${{ env.WORKING_DIRECTORY }}
          out: out/coverage.json

      - name: Ensure summary.line is set in coverage.json (Go)
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          set -euo pipefail

          if [ ! -f "coverage.out" ]; then
            echo "❌ coverage.out not found – did make test run 'go test ... -coverprofile=coverage.out'?"
            ls -la
            exit 1
          fi

          go tool cover -func=coverage.out > coverage-func.txt
          tail -n 5 coverage-func.txt || true

          python - << 'PY'
          import json
          from pathlib import Path

          lines = Path("coverage-func.txt").read_text(encoding="utf-8").strip().splitlines()
          last = lines[-1] if lines else ""
          # expected: "total: (statements) 88.0%"
          pct = float(last.split()[-1].rstrip("%"))

          out = Path("out")
          out.mkdir(parents=True, exist_ok=True)
          cov_path = out / "coverage.json"

          cov = json.loads(cov_path.read_text(encoding="utf-8")) if cov_path.exists() else {}
          cov.setdefault("summary", {})
          cov["summary"]["line"] = pct
          cov.setdefault("files", [])

          cov_path.write_text(json.dumps(cov, indent=2), encoding="utf-8")
          print(f"✅ summary.line set to {pct:.1f}")
          PY

          cat out/coverage.json

      - name: Upload Go coverage artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          # per-shard artifact name (so merge job can pattern match)
          name: coverage-go-shard${{ env.SHARD_INDEX }}
          path: |
            ${{ env.WORKING_DIRECTORY }}/out/coverage.json
            ${{ env.WORKING_DIRECTORY }}/coverage.out
            ${{ env.WORKING_DIRECTORY }}/coverage-func.txt
            ${{ env.WORKING_DIRECTORY }}/out/junit.xml
            ${{ env.WORKING_DIRECTORY }}/out/shard-files.txt
            ${{ env.WORKING_DIRECTORY }}/out/shard-packages.txt
          if-no-files-found: ignore
          include-hidden-files: true
          retention-days: 14
