# =============================================================================
# BrikByteOS — Reusable Node.js Unit Test Workflow
# -----------------------------------------------------------------------------
# WBS ID : PIPE-TEST-UNIT-CI-BUILD-002
# Repos  :
#   - Implemented in:
#       • BrikByte-Studios/.github (.github/workflows/test-node.yml)
#   - Consumed by:
#       • BrikByte-Studios/brik-pipe-examples/node-api-example
#       • Product repos (via workflow_call)
#
# Contract:
#   - Expects repo to expose:
#       • Makefile with `make test` (per PIPE-TEST-UNIT-STANDARDS-INIT-001)
#   - This workflow:
#       • Plans a governed matrix (shards) via matrix-plan
#       • Sets up Node
#       • Uses cache-node-deps composite
#       • Runs `make test` per shard
#       • Normalizes + uploads coverage per shard
# =============================================================================

name: "BrikByteOS — Node.js Unit Tests"

on:
  workflow_call:
    inputs:
      node-version:
        description: "Node.js version (must align with Runtime Matrix)"
        required: false
        type: string
        default: "20"

      working-directory:
        description: "Path to Node project (folder with package.json + Makefile)"
        required: false
        type: string
        default: "."

      # -----------------------------------------------------------------------
      # Parallelization / Matrix (governed)
      # -----------------------------------------------------------------------
      override_shards:
        description: >
          Requested shard count for unit tests.
          Will be clamped to caps from parallel-matrix.yml.
        required: false
        type: string
        default: ""

      matrix_config_path:
        description: >
          Optional path to parallel-matrix.yml.
          Leave empty to use action defaults (inside .github repo).
        required: false
        type: string
        default: ""

permissions:
  contents: read

jobs:
  # ----------------------------------------------------------------------------
  # PLAN: governed deterministic matrix (unit shards)
  # ----------------------------------------------------------------------------
  plan:
    name: "Plan governed unit matrix"
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.plan.outputs.matrix_json }}
    steps:
      - name: "Checkout (caller repo)"
        uses: actions/checkout@v4

      - name: "Generate unit shard matrix (governed)"
        id: plan
        uses: BrikByte-Studios/.github/.github/actions/matrix-plan@main
        with:
          test_type: unit
          config_path: ${{ inputs.matrix_config_path }}
          override_shards: ${{ inputs.override_shards }}

  # ----------------------------------------------------------------------------
  # MATRIX: shard
  # ----------------------------------------------------------------------------
  test:
    name: "Node.js: Unit Tests • shard ${{ matrix.shard }}"
    needs: plan
    runs-on: ubuntu-latest
    timeout-minutes: 25

    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.plan.outputs.matrix) }}

    env:
      LANGUAGE: node
      NODE_VERSION: ${{ inputs.node-version || '20' }}
      WORKING_DIRECTORY: ${{ inputs.working-directory || '.' }}

      # -----------------------------------------------------------------------
      # Shard contract (runner must honor this to actually split work)
      # -----------------------------------------------------------------------
      UNIT_SHARD: ${{ matrix.shard }}
      UNIT_SHARD_TOTAL: ${{ strategy.job-total }}

      FORCE_COLOR: "0"

    steps:
      # -----------------------------------------------------------------------
      # 1) Checkout repository
      # -----------------------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # -----------------------------------------------------------------------
      # 2) Verify Node project layout
      # -----------------------------------------------------------------------
      - name: Verify Node test layout
        run: |
          set -euo pipefail
          echo "📁 Working directory: ${WORKING_DIRECTORY}"

          if [ ! -d "${WORKING_DIRECTORY}" ]; then
            echo "❌ WORKING_DIRECTORY '${WORKING_DIRECTORY}' does not exist."
            exit 1
          fi

          if [ ! -f "${WORKING_DIRECTORY}/package.json" ]; then
            echo "❌ package.json not found in ${WORKING_DIRECTORY}."
            exit 1
          fi

          if [ ! -f "${WORKING_DIRECTORY}/Makefile" ]; then
            echo "❌ Makefile not found in ${WORKING_DIRECTORY}."
            echo "   Expected a Makefile exposing 'make test' (Node)."
            exit 1
          fi

      # -----------------------------------------------------------------------
      # 3) Setup Node.js runtime
      # -----------------------------------------------------------------------
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      # -----------------------------------------------------------------------
      # 4) Cache deps
      # -----------------------------------------------------------------------
      - name: "Cache Node dependencies (npm / Yarn / pnpm)"
        uses: BrikByte-Studios/.github/.github/actions/cache-node-deps@main
        with:
          node-version: ${{ env.NODE_VERSION }}
          project-path: ${{ env.WORKING_DIRECTORY }}

      # -----------------------------------------------------------------------
      # 5) Run unit tests via `make test` (per shard)
      # -----------------------------------------------------------------------
      - name: Run Node.js unit tests (make test)
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          set -euo pipefail

          echo "🧪 [TEST] Language     : ${LANGUAGE}"
          echo "🧪 [TEST] Runtime      : Node.js ${NODE_VERSION}"
          echo "🧪 [TEST] Directory    : ${PWD}"
          echo "🧪 [TEST] Command      : make test"
          echo "🧪 [TEST] Unit shard   : ${UNIT_SHARD}/${UNIT_SHARD_TOTAL}"
          echo "🧪 [TEST] Status       : STARTING"

          START_TS=$(date +%s)

          # IMPORTANT:
          # Your Makefile / test runner must read UNIT_SHARD + UNIT_SHARD_TOTAL
          # to actually split test files deterministically.
          make test

          END_TS=$(date +%s)
          DURATION=$((END_TS - START_TS))

          echo "✅ [TEST] Status       : COMPLETED"
          echo "⏱  [TEST] Duration     : ${DURATION} seconds"

      # -----------------------------------------------------------------------
      # 6) Normalize coverage → out/coverage.json
      # -----------------------------------------------------------------------
      - name: Normalize coverage → coverage.json
        uses: BrikByte-Studios/.github/.github/actions/coverage-merge@main
        with:
          language: ${{ env.LANGUAGE }}
          working-directory: ${{ env.WORKING_DIRECTORY }}
          out: out/coverage.json

      - name: Ensure summary.line is set in coverage.json (Node + c8 json-summary)
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          set -euo pipefail
          echo "🔧 Normalizing coverage: syncing summary.line from coverage/coverage-summary.json"

          if [ ! -f "coverage/coverage-summary.json" ]; then
            echo "❌ coverage/coverage-summary.json not found – did c8 run with --reporter=json-summary?"
            ls -R
            exit 1
          fi

          node - << 'EOF'
          const fs = require('fs');

          function readJson(path) {
            return JSON.parse(fs.readFileSync(path, 'utf8'));
          }

          const summary = readJson('coverage/coverage-summary.json');

          if (!summary.total || !summary.total.lines || typeof summary.total.lines.pct !== 'number') {
            console.error('Unexpected coverage-summary.json format. total.lines.pct is missing.');
            process.exit(1);
          }

          const linePct = summary.total.lines.pct;

          let cov;
          try { cov = readJson('out/coverage.json'); }
          catch { cov = {}; }

          cov.summary = (cov.summary && typeof cov.summary === 'object') ? cov.summary : {};
          cov.summary.line = linePct;

          if (!Array.isArray(cov.files)) cov.files = [];

          fs.writeFileSync('out/coverage.json', JSON.stringify(cov, null, 2));
          console.log('✅ Updated out/coverage.json with summary.line =', linePct);
          EOF

          echo "✅ coverage.json after normalization:"
          cat out/coverage.json

      # -----------------------------------------------------------------------
      # 7) Upload coverage artifacts (per shard)
      # -----------------------------------------------------------------------
      - name: Upload Node coverage artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ env.LANGUAGE }}-shard${{ matrix.shard }}-node${{ env.NODE_VERSION }}-${{ github.run_id }}
          path: |
            ${{ env.WORKING_DIRECTORY }}/out/coverage.json
            ${{ env.WORKING_DIRECTORY }}/coverage/
            ${{ env.WORKING_DIRECTORY }}/out/junit.xml
          if-no-files-found: warn
          include-hidden-files: true
          retention-days: 14
