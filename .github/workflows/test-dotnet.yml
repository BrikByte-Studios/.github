# =============================================================================
# BrikByteOS — Reusable .NET Unit Test Workflow
# -----------------------------------------------------------------------------
# WBS ID : PIPE-TEST-UNIT-CI-BUILD-002
# Repos  :
#   - Implemented in:
#       • BrikByte-Studios/.github (.github/workflows/test-dotnet.yml)
#   - Consumed by:
#       • BrikByte-Studios/brik-pipe-examples/dotnet-api-example
#       • Product repos (via workflow_call)
#
# Contract:
#   - Expects:
#       • Makefile with `make test` calling `dotnet test`
#       • One or more test projects (e.g. /tests/SomeApp.Tests).
# =============================================================================
name: "BrikByteOS — .NET Unit Tests"

on:
  workflow_call:
    inputs:
      dotnet-version:
        description: ".NET SDK version (e.g. 8.0.x)"
        required: false
        type: string
        default: "8.0.x"
      working-directory:
        description: "Path to .NET solution root (Makefile + *.sln or tests/)"
        required: false
        type: string
        default: "."

permissions:
  contents: read

jobs:
  test:
    name: ".NET: Unit Tests"
    runs-on: ubuntu-latest

    env:
      LANGUAGE: dotnet
      DOTNET_VERSION: ${{ inputs.dotnet-version != '' && inputs.dotnet-version || '8.0.x' }}
      WORKING_DIRECTORY: ${{ inputs.working-directory != '' && inputs.working-directory || '.' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify .NET test layout
        run: |
          echo "📁 Working directory: ${WORKING_DIRECTORY}"

          if [ ! -d "${WORKING_DIRECTORY}" ]; then
            echo "❌ WORKING_DIRECTORY '${WORKING_DIRECTORY}' does not exist."
            exit 1
          fi

          cd "${WORKING_DIRECTORY}"

          if ! compgen -G "*.sln" > /dev/null && ! compgen -G "**/*.csproj" > /dev/null; then
            echo "❌ No .sln or .csproj files found in ${PWD}."
            exit 1
          fi

          if [ ! -f "Makefile" ]; then
            echo "❌ Makefile not found in ${PWD}."
            echo "   Expected 'make test' mapping to 'dotnet test'."
            exit 1
          fi

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: "Cache .NET dependencies & tools"
        uses: BrikByte-Studios/.github/.github/actions/cache-dotnet-deps@main
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          project-path: ${{ env.WORKING_DIRECTORY }}

      - name: Run .NET unit tests (make test)
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          set -euo pipefail

          echo "🧪 [TEST] Language  : ${LANGUAGE}"
          echo "🧪 [TEST] Runtime   : .NET SDK ${DOTNET_VERSION}"
          echo "🧪 [TEST] Directory : ${PWD}"
          echo "🧪 [TEST] Command   : make test"
          echo "🧪 [TEST] Status    : STARTING"

          START_TS=$(date +%s)

          make test

          END_TS=$(date +%s)
          DURATION=$((END_TS - START_TS))

          echo "✅ [TEST] Status    : COMPLETED"
          echo "⏱  [TEST] Duration : ${DURATION} seconds"
          echo "ℹ️  [TEST] See dotnet test summary above."

      # -----------------------------------------------------------------------
      # X) Collect coverage (.NET: coverlet JSON via dotnet test)
      # -----------------------------------------------------------------------
      - name: Normalize coverage → coverage.json
        uses: BrikByte-Studios/.github/.github/actions/coverage-merge@main
        with:
          language: ${{ env.LANGUAGE }}
          working-directory: ${{ env.WORKING_DIRECTORY }}
          out: out/coverage.json

      - name: Ensure summary.line is set in coverage.json (.NET / Coverlet)
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          set -euo pipefail
          echo "🔧 Normalizing .NET coverage: syncing summary.line from Coverlet JSON"

          # 1) Locate Coverlet JSON (e.g. TestResults/coverage.net.json)
          if [ -f "TestResults/coverage.net.json" ]; then
            export COVERLET_JSON="TestResults/coverage.net.json"
          else
            echo "❌ Coverlet JSON not found at TestResults/coverage.net.json; searching..."
            FOUND=$(find TestResults -maxdepth 2 -type f -name "*.json" | head -n 1 || true)
            if [ -z "$FOUND" ]; then
              echo "❌ No Coverlet JSON file found under TestResults/"
              ls -R
              exit 1
            fi
            echo "ℹ️ Using detected Coverlet JSON: $FOUND"
            export COVERLET_JSON="$FOUND"
          fi

          python - << 'EOF'
          import json
          import os
          from pathlib import Path

          coverlet_path = Path(os.environ["COVERLET_JSON"])
          data = json.loads(coverlet_path.read_text(encoding="utf-8"))

          # Try several shapes Coverlet uses.
          percent = None
          total = None
          covered = None

          # Shape 1: top-level "Line": { Total, Covered, Percent }
          line_summary = data.get("Line")
          if isinstance(line_summary, dict):
            percent = line_summary.get("Percent")
            total = line_summary.get("Total")
            covered = line_summary.get("Covered")

          # Shape 2: "Summary": { "Line": {...} }
          if percent is None and isinstance(data.get("Summary"), dict):
            ls = data["Summary"].get("Line") or data["Summary"].get("line")
            if isinstance(ls, dict):
              percent = ls.get("Percent") or ls.get("percent")
              total = ls.get("Total") or ls.get("total")
              covered = ls.get("Covered") or ls.get("covered")

          # Shape 3: Aggregate from Modules[*].Summary.Line
          if percent is None and "Modules" in data and isinstance(data["Modules"], dict):
            t_total = 0
            t_cov = 0
            for mod in data["Modules"].values():
              summary = mod.get("Summary") or mod.get("summary") or {}
              ls = summary.get("Line") or summary.get("line") or {}
              tt = ls.get("Total") or ls.get("total")
              cc = ls.get("Covered") or ls.get("covered")
              if isinstance(tt, (int, float)) and isinstance(cc, (int, float)):
                t_total += tt
                t_cov += cc
            if t_total > 0:
              total = t_total
              covered = t_cov
              percent = (t_cov / t_total) * 100.0

          if percent is None:
            raise SystemExit("Cannot determine coverage percent from Coverlet JSON (no Line summary found)")

          cov_dir = Path("out")
          cov_dir.mkdir(parents=True, exist_ok=True)
          cov_path = cov_dir / "coverage.json"

          try:
            cov = json.loads(cov_path.read_text(encoding="utf-8"))
          except FileNotFoundError:
            cov = {}

          cov.setdefault("summary", {})
          cov["summary"]["line"] = float(percent)

          # Ensure files is present (even if empty)
          cov.setdefault("files", [])

          cov_path.write_text(json.dumps(cov, indent=2), encoding="utf-8")
          print(f"✅ Updated out/coverage.json with summary.line = {percent:.2f}")
          EOF

          echo "✅ coverage.json after normalization:"
          cat out/coverage.json

      - name: Upload Dotnet coverage artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ env.LANGUAGE }}
          path: |
            ${{ env.WORKING_DIRECTORY }}/out/coverage.json
            ${{ env.WORKING_DIRECTORY }}/TestResults/
          if-no-files-found: ignore
