# =============================================================================
# BrikByteOS — Reusable .NET Unit Test Workflow (sharded)
# -----------------------------------------------------------------------------
# WBS ID : PIPE-TEST-UNIT-CI-BUILD-002
# Repos  :
#   - Implemented in:
#       • BrikByte-Studios/.github (.github/workflows/test-dotnet.yml)
#   - Consumed by:
#       • BrikByte-Studios/brik-pipe-examples/dotnet-api-example
#       • Product repos (via workflow_call)
#
# Contract:
#   - Expects:
#       • Makefile with `make test` calling `dotnet test`
#       • One or more test projects (e.g. /tests/SomeApp.Tests)
#
# Matrix plan integration:
#   - Uses BrikPipe Matrix Plan composite action to compute shards deterministically
#   - Parallel rules live in:
#       BrikByte-Studios/.github/.github/actions/matrix-plan/parallel-matrix.yml
# =============================================================================

name: "BrikByteOS — .NET Unit Tests"

on:
  workflow_call:
    inputs:
      dotnet-version:
        description: ".NET SDK version (e.g. 8.0.x)"
        required: false
        type: string
        default: "8.0.x"

      working-directory:
        description: "Path to .NET solution root (Makefile + *.sln or tests/)"
        required: false
        type: string
        default: "."

      # ------------------------------
      # Governed parallelism (optional)
      # ------------------------------
      override_shards:
        description: "Optional requested shard count (clamped to caps)."
        required: false
        type: string
        default: ""

      matrix_config_path:
        description: >
          Optional path to parallel-matrix.yml.
          Leave empty to use default inside BrikByte-Studios/.github.
        required: false
        type: string
        default: ""

permissions:
  contents: read

jobs:
  # ---------------------------------------------------------------------------
  # 0) PLAN: compute governed matrix JSON (job output)
  # ---------------------------------------------------------------------------
  plan:
    name: "Matrix Plan (.NET unit)"
    runs-on: ubuntu-latest
    outputs:
      matrix_json: ${{ steps.matrix.outputs.matrix_json }}

    steps:
      - name: "Compute matrix plan (unit)"
        id: matrix
        uses: BrikByte-Studios/.github/.github/actions/matrix-plan@main
        with:
          test_type: "unit"
          config_path: ${{ inputs.matrix_config_path }}
          override_shards: ${{ inputs.override_shards }}

      - name: "Debug matrix_json"
        run: |
          echo "matrix_json=${{ steps.matrix.outputs.matrix_json }}"

  # ---------------------------------------------------------------------------
  # 1) TEST: run shards from matrix computed in plan job
  # ---------------------------------------------------------------------------
  test:
    name: ".NET: Unit Tests • shard ${{ matrix.shard }}"
    runs-on: ubuntu-latest
    needs: plan

    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.plan.outputs.matrix_json) }}

    env:
      LANGUAGE: dotnet
      DOTNET_VERSION: ${{ inputs.dotnet-version != '' && inputs.dotnet-version || '8.0.x' }}
      WORKING_DIRECTORY: ${{ inputs.working-directory != '' && inputs.working-directory || '.' }}

      # shard metadata (deterministic)
      SHARD_INDEX: ${{ matrix.shard }}
      SHARD_TOTAL: ${{ strategy.job-total }}

      # Optional sharding contract for Makefile/test runners
      UNIT_SHARD: ${{ matrix.shard }}
      UNIT_SHARD_TOTAL: ${{ strategy.job-total }}

      FORCE_COLOR: "0"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify .NET test layout
        run: |
          set -euo pipefail
          echo "📁 Working directory: ${WORKING_DIRECTORY}"

          if [ ! -d "${WORKING_DIRECTORY}" ]; then
            echo "❌ WORKING_DIRECTORY '${WORKING_DIRECTORY}' does not exist."
            exit 1
          fi

          cd "${WORKING_DIRECTORY}"

          # NOTE: Bash globstar isn't enabled by default; keep checks simple & reliable.
          if ! ls *.sln >/dev/null 2>&1 && ! find . -maxdepth 4 -name "*.csproj" | head -n 1 | grep -q .; then
            echo "❌ No .sln or .csproj files found under ${PWD}."
            exit 1
          fi

          if [ ! -f "Makefile" ]; then
            echo "❌ Makefile not found in ${PWD}."
            echo "   Expected 'make test' mapping to 'dotnet test'."
            exit 1
          fi

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: "Cache .NET dependencies & tools"
        uses: BrikByte-Studios/.github/.github/actions/cache-dotnet-deps@main
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          project-path: ${{ env.WORKING_DIRECTORY }}

      - name: Run .NET unit tests (make test)
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          set -euo pipefail

          echo "🧪 [TEST] Language  : ${LANGUAGE}"
          echo "🧪 [TEST] Runtime   : .NET SDK ${DOTNET_VERSION}"
          echo "🧪 [TEST] Directory : ${PWD}"
          echo "🧪 [TEST] Command   : make test"
          echo "🧪 [TEST] Shard     : ${UNIT_SHARD}/${UNIT_SHARD_TOTAL}"
          echo "🧪 [TEST] Status    : STARTING"

          START_TS=$(date +%s)

          # NOTE:
          # Your Makefile may optionally consume UNIT_SHARD / UNIT_SHARD_TOTAL.
          # If it doesn't, all shards will run the full suite (wasteful but correct).
          make test

          END_TS=$(date +%s)
          DURATION=$((END_TS - START_TS))

          echo "✅ [TEST] Status    : COMPLETED"
          echo "⏱  [TEST] Duration : ${DURATION} seconds"
          echo "ℹ️  [TEST] See dotnet test summary above."

      # -----------------------------------------------------------------------
      # Coverage normalization (your existing approach)
      # -----------------------------------------------------------------------
      - name: Normalize coverage → coverage.json
        uses: BrikByte-Studios/.github/.github/actions/coverage-merge@main
        with:
          language: ${{ env.LANGUAGE }}
          working-directory: ${{ env.WORKING_DIRECTORY }}
          out: out/coverage.json

      - name: Ensure summary.line is set in coverage.json (.NET / Coverlet)
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          set -euo pipefail
          echo "🔧 Normalizing .NET coverage: syncing summary.line from Coverlet JSON"

          # Prefer deterministic shard-specific file if your Makefile emits it.
          FOUND=$(find . -type f -name "coverage.net*.json" | head -n 1 || true)
          if [ -z "$FOUND" ]; then
            echo "❌ No Coverlet JSON file found under working directory."
            ls -R
            exit 1
          fi
          echo "ℹ️ Using detected Coverlet JSON: $FOUND"
          export COVERLET_JSON="$FOUND"

          python - << 'PY'
          import json, os
          from pathlib import Path

          cov_dir = Path("out")
          cov_dir.mkdir(parents=True, exist_ok=True)
          cov_path = cov_dir / "coverage.json"

          cov = json.loads(cov_path.read_text(encoding="utf-8")) if cov_path.exists() else {}

          coverlet_path = Path(os.environ["COVERLET_JSON"])
          data = json.loads(coverlet_path.read_text(encoding="utf-8"))

          percent = None

          # --- Coverlet: Summary.LineCoverage (most common) -------------------
          if percent is None and isinstance(data.get("Summary"), dict):
            s = data["Summary"]
            percent = (
              s.get("LineCoverage") or s.get("lineCoverage") or
              s.get("SequenceCoverage") or s.get("sequenceCoverage")
            )

          # --- Aggregate from modules if Summary missing ----------------------
          if percent is None:
            modules = data.get("Modules")
            if isinstance(modules, dict):
              module_iter = modules.values()
            elif isinstance(modules, list):
              module_iter = modules
            else:
              module_iter = []

            total_lines = 0
            covered_lines = 0

            for mod in module_iter:
              if not isinstance(mod, dict):
                continue
              s = mod.get("Summary")
              if not isinstance(s, dict):
                continue

              num = s.get("NumLines") or s.get("numLines")
              covd = s.get("CoveredLines") or s.get("coveredLines")
              if isinstance(num, (int, float)) and isinstance(covd, (int, float)):
                total_lines += int(num)
                covered_lines += int(covd)

            if total_lines > 0:
              percent = (covered_lines / total_lines) * 100.0

          if percent is None:
            raise SystemExit("Cannot determine coverage percent from Coverlet JSON")

          cov.setdefault("summary", {})
          cov["summary"]["line"] = float(percent)
          cov.setdefault("files", [])

          cov_path.write_text(json.dumps(cov, indent=2), encoding="utf-8")
          print(f"✅ Updated out/coverage.json with summary.line = {float(percent):.2f}")
          PY

          cat out/coverage.json


      - name: Upload Dotnet coverage artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          # If you shard, make this unique per shard (or merge later).
          name: coverage-dotnet-shard${{ env.UNIT_SHARD }}
          path: |
            ${{ env.WORKING_DIRECTORY }}/out/coverage.json
            ${{ env.WORKING_DIRECTORY }}/TestResults/
            ${{ env.WORKING_DIRECTORY }}/out/junit-shard${{ env.UNIT_SHARD }}.xml
          if-no-files-found: ignore
